# AGENTS.md — restful-api `dto` tests

Rules: `../AGENTS.md` (module test guide) and the main-side `dto/AGENTS.md`. Local:
test record construction paths — convenience-constructor defaults (empty details,
timestamp windows asserted between before/after instants), canonical constructor,
equals/hashCode, and the defensive copy (mutating the caller's list must not affect
the record).
