spring:
  batch:
    jdbc:
      initialize-schema: always   # auto-create batch metadata tables
    job:
      enabled: false              # don't auto-run jobs on startup
  datasource:
    url: jdbc:h2:mem:batchdb
    driver-class-name: org.h2.Driver
